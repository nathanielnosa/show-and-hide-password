const pwd = document.querySelector('#pwd');
const seepwd = document.querySelector('#see');

seepwd.addEventListener('click',()=>{

  (pwd.type !=='password')?
  document.body.innerHTML = "Enter a text"
  :console.log(pwd.type =Text);

});
